---
import Layout from '../layouts/Layout.astro';
import { teamMembers, companyValues } from '../data/team';
---

<Layout title="Das Team – Kreuzer Trockenbau & Malerbetrieb" description="Lernen Sie das Team hinter Kreuzer Trockenbau kennen: Markus Kreuzer (Gründer) und Tobias Kreuzer (Malermeister).">

  <!-- Page Header -->
  <section class="pt-32 pb-16 sm:pt-40 sm:pb-20 bg-dark-950 relative overflow-hidden">
    <div class="absolute top-0 left-1/2 -translate-x-1/2 w-[600px] h-[400px] bg-accent-500/5 rounded-full blur-3xl"></div>
    <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <span class="text-accent-500 text-sm font-medium uppercase tracking-widest fade-in">Wer wir sind</span>
      <h1 class="text-4xl sm:text-5xl lg:text-6xl font-bold text-white mt-3 mb-6 fade-in font-heading">Das Team</h1>
      <p class="text-dark-300 text-lg max-w-2xl mx-auto fade-in">
        Bei Kreuzer Trockenbau & Malerbetrieb steht der Kunde immer an erster Stelle.
      </p>
    </div>
  </section>

  <!-- Team Members -->
  <section class="py-16 sm:py-24 bg-dark-950">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
        {teamMembers.map((member, index) => (
          <div class:list={[
            'p-8 sm:p-10 rounded-2xl bg-dark-800/50 border border-white/5 hover:border-accent-500/20 transition-all duration-500',
            index === 0 ? 'fade-in-left' : 'fade-in-right',
          ]}>
            <!-- Avatar Placeholder -->
            <div class="w-20 h-20 rounded-2xl bg-accent-500/10 border border-accent-500/20 flex items-center justify-center mb-6">
              <span class="text-accent-500 font-bold text-2xl">{member.name.split(' ').map(n => n[0]).join('')}</span>
            </div>
            <h2 class="text-2xl font-bold text-white mb-1 font-heading">{member.name}</h2>
            <p class="text-accent-500 font-medium mb-5">{member.role}</p>
            <p class="text-dark-300 leading-relaxed">{member.description}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Company Story -->
  <section class="py-16 sm:py-24 bg-dark-900">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center">
        <div class="fade-in-left">
          <span class="text-accent-500 text-sm font-medium uppercase tracking-widest">Unsere Geschichte</span>
          <h2 class="text-3xl sm:text-4xl font-bold text-white mt-3 mb-6 font-heading">
            Seit 2010 Ihr Partner für Qualität
          </h2>
          <div class="space-y-4 text-dark-300 leading-relaxed">
            <p>
              Kreuzer Trockenbau wurde 2010 gegründet und ist in Trockenbau und Innenausbau spezialisiert. 2017 konnten wir unsere Firma mit einem Malermeisterbetrieb erweitern.
            </p>
            <p>
              Es ist uns sehr wichtig, unseren Kunden eine hohe Qualität an Material und handwerklicher Ausführung bieten zu können.
            </p>
            <p>
              Höchste Priorität von Kreuzer Trockenbau & Malerbetrieb ist die Zufriedenheit unserer Kunden. Sie sind uns sehr wichtig und können sich jederzeit darauf verlassen, dass wir keine Anstrengungen scheuen, wenn es um Ihre Baumaßnahme geht.
            </p>
            <p>
              Eine erstklassige Kundenbetreuung ist unser Kennzeichen und wir sind stolz darauf, für Sie zu arbeiten und bieten Ihnen Komplettlösungen aus einer Hand für Ihr "kleines" oder "großes" Bauprojekt.
            </p>
          </div>
        </div>

        <!-- Values -->
        <div class="fade-in-right">
          <div class="p-8 sm:p-10 rounded-2xl bg-dark-800/50 border border-white/5">
            <h3 class="text-xl font-bold text-white mb-8 font-heading">Dafür stehen wir</h3>
            <div class="space-y-6">
              {companyValues.map((value, index) => (
                <div class="flex items-center gap-4">
                  <div class="w-10 h-10 rounded-xl bg-accent-500/10 flex items-center justify-center shrink-0">
                    <span class="text-accent-500 font-bold text-sm">{String(index + 1).padStart(2, '0')}</span>
                  </div>
                  <span class="text-dark-200 font-medium">{value}</span>
                </div>
              ))}
            </div>
          </div>

          <!-- Timeline -->
          <div class="mt-8 p-8 sm:p-10 rounded-2xl bg-dark-800/50 border border-white/5">
            <h3 class="text-xl font-bold text-white mb-8 font-heading">Meilensteine</h3>
            <div class="space-y-6">
              <div class="flex items-start gap-4">
                <div class="w-10 h-10 rounded-xl bg-accent-500 flex items-center justify-center shrink-0">
                  <span class="text-dark-950 font-bold text-xs">2010</span>
                </div>
                <div>
                  <p class="text-white font-medium">Firmengründung</p>
                  <p class="text-dark-400 text-sm">Gründung durch Markus Kreuzer, Spezialisierung auf Trockenbau</p>
                </div>
              </div>
              <div class="flex items-start gap-4">
                <div class="w-10 h-10 rounded-xl bg-accent-500 flex items-center justify-center shrink-0">
                  <span class="text-dark-950 font-bold text-xs">2017</span>
                </div>
                <div>
                  <p class="text-white font-medium">Erweiterung</p>
                  <p class="text-dark-400 text-sm">Tobias Kreuzer erweitert den Betrieb um den Malermeisterbetrieb</p>
                </div>
              </div>
              <div class="flex items-start gap-4">
                <div class="w-10 h-10 rounded-xl bg-accent-500/20 border border-accent-500/30 flex items-center justify-center shrink-0">
                  <span class="text-accent-500 font-bold text-xs">Heute</span>
                </div>
                <div>
                  <p class="text-white font-medium">Komplettanbieter</p>
                  <p class="text-dark-400 text-sm">Komplettlösungen aus einer Hand für Trockenbau und Innenausbau</p>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

</Layout>
